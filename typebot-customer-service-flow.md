# تدفق Typebot — روبوت خدمة العملاء الأولي (BOTSHOP-DZ)

> هذا وصف بنيوي دقيق للتدفق (Blocks + Logic + Variables) لبنائه مباشرة داخل محرر Typebot المرئي.
> لم أُرفق ملف تصدير Typebot جاهزاً للاستيراد لأن صيغة التصدير الداخلية لـ Typebot ترتبط بإصدار المنصة ومعرّفات فريدة لكل حساب، وتوليدها يدوياً هنا قد يُنتج ملفاً غير صالح للاستيراد. البناء المباشر عبر المحرر (10–15 دقيقة) أضمن وأدق.

---

## 1. الهدف
بوت مضمّن في صفحة المتجر (Storefront)، يجيب عن الأسئلة الشائعة، ويجمع بيانات العميل الأساسية، ثم يُسلّم المحادثة لتاجر عبر واتساب (webhook إلى n8n) عندما يتجاوز الطلب قدرة الرد الآلي.

## 2. المتغيرات (Variables) المطلوبة في Typebot
| المتغير | الوصف |
|---|---|
| `customer_name` | اسم العميل |
| `customer_phone` | رقم هاتف العميل |
| `store_id` | مُمرَّر عبر رابط التضمين (Query Param) من صفحة المتجر |
| `intent` | نية العميل المُختارة من القائمة (سؤال / طلب منتج / تتبع طلب) |
| `product_of_interest` | اسم المنتج الذي يسأل عنه العميل (نص حر أو اختيار) |

## 3. مخطط التدفق (Blocks بالتسلسل)

**البداية (Start)**
→ رسالة ترحيب: "أهلاً بك 👋 أنا المساعد الآلي لمتجرك. كيف يمكنني مساعدتك اليوم؟"
→ Choice Input (اختيار واحد): [الأسئلة الشائعة] [أسأل عن منتج] [تتبّع طلبي] [التحدث مع التاجر مباشرة]

---

### مسار "الأسئلة الشائعة"
→ Choice Input: [ما هي وسائل الدفع المتاحة؟] [كم مدة التوصيل؟] [كيف يمكنني الإرجاع؟] [رجوع للقائمة الرئيسية]
- كل خيار → Text Bubble برد جاهز (يُعبّأ يدوياً حسب سياسة كل متجر)، ثم Choice Input بـ "هل أجبت على سؤالك؟ [نعم] [أريد التحدث مع التاجر]"
- "أريد التحدث مع التاجر" → ينتقل لمسار "التحدث مع التاجر مباشرة" أدناه

---

### مسار "أسأل عن منتج"
→ Text Input (حفظ في `product_of_interest`): "ما اسم المنتج الذي تسأل عنه؟"
→ Text Bubble: "شكراً 🙏 دعني أتحقق من التوفر..."
→ **Webhook Block** (HTTP GET) → `{{n8n_base_url}}/webhook/botshop-typebot-product-lookup?store_id={{store_id}}&query={{product_of_interest}}`
  - يستدعي سير عمل n8n يبحث في جدول `products` بـ Supabase عن تطابق نصي، ويُعيد JSON: `{ found: boolean, name, price, stock }`
→ Condition Block: إن `found = true` → Text Bubble ديناميكي يعرض السعر والمخزون، ثم ينتقل لمسار "جمع بيانات الطلب"
→ إن `found = false` → Text Bubble: "لم أجد هذا المنتج تحديداً، سأحوّلك للتاجر مباشرة" → ينتقل لمسار "التحدث مع التاجر مباشرة"

---

### مسار "تتبّع طلبي"
→ Text Input (حفظ في متغير مؤقت `order_id_query`): "أرسل رقم طلبك (مثال: ORD-10432)"
→ **Webhook Block** (HTTP GET) → `{{n8n_base_url}}/webhook/botshop-typebot-order-status?order_id={{order_id_query}}`
→ Text Bubble ديناميكي يعرض حالة الطلب المُعادة من الاستجابة

---

### مسار "جمع بيانات الطلب" (بعد تأكيد رغبة العميل بالشراء)
→ Text Input (حفظ في `customer_name`): "ما اسمك الكامل؟"
→ Phone Number Input (حفظ في `customer_phone`، مع تحقق من صيغة جزائرية 05/06/07): "رقم هاتفك للتواصل؟"
→ Text Bubble: "تمام ✅ سيتواصل معك التاجر الآن لتأكيد الطلب."
→ **Webhook Block** (HTTP POST) → `{{n8n_base_url}}/webhook/botshop-typebot-handoff`
  - Body: `{ store_id, customer_name, customer_phone, product_of_interest, intent: "order" }`
  - يستدعي سير عمل n8n يُنشئ سجلاً أولياً ويُرسل إشعار واتساب فورياً للتاجر (يُبنى بنفس منطق `n8n-01-order-notification.json` مع تعديل المصدر)

---

### مسار "التحدث مع التاجر مباشرة"
→ Text Input (حفظ في `customer_name`) إن لم يكن محفوظاً بعد
→ Phone Number Input (حفظ في `customer_phone`) إن لم يكن محفوظاً بعد
→ **Webhook Block** (HTTP POST) → نفس `botshop-typebot-handoff` أعلاه مع `intent: "direct_contact"`
→ Text Bubble ختامي: "شكراً، سيصلك رد من فريق المتجر خلال دقائق عبر واتساب 💬"

---

## 4. إعدادات التضمين (Embed) في صفحة المتجر
- يُضمَّن Typebot في `04-storefront.html` عبر Standard Embed أو Bubble Embed الرسمي من Typebot (سكربت `<script>` جاهز من لوحة تحكم Typebot نفسها).
- يُمرَّر `store_id` تلقائياً كـ Query Param عند فتح المحادثة، حتى يعرف كل Webhook أي متجر يخص الطلب.

## 5. ملاحظة هامة
- الردود الجاهزة (نصوص الأسئلة الشائعة، سياسة الإرجاع الفعلية لكل متجر) **لم تُكتب هنا بمحتوى نهائي مُختلَق**، بل تُركت كحقول تُعبَّأ من صاحب كل متجر حسب سياسته الفعلية، تفادياً لأي معلومة غير دقيقة قد يعتمدها عميل حقيقي.
