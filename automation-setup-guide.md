# دليل إعداد الأتمتة — BOTSHOP-DZ
## Supabase × n8n × Typebot × WhatsApp Cloud API × Vercel

---

## 1. نظرة عامة على التدفق

```
عميل يتصفح المتجر (04-storefront.html على Vercel)
        │
        ├─▶ Typebot (مُضمَّن) ── يجيب عن الأسئلة ويجمع بيانات الطلب
        │
        ▼
   إنشاء طلب في Supabase (جدول orders)
        │
        ▼
  Supabase Database Webhook (INSERT trigger)
        │
        ▼
      n8n (سير العمل 01) ── يتحقق من القائمة السوداء ويُرسل إشعار واتساب فورياً للتاجر
```

---

## 2. الخطوات العملية بالترتيب

### أ) Supabase
1. نفّذ ملف `botshop-dz-schema.sql` كاملاً عبر SQL Editor في مشروع Supabase.
2. من **Database → Webhooks**، أنشئ 3 Webhooks تستدعي روابط n8n (تُنشأ في الخطوة التالية):
   - على جدول `orders`، الحدث `INSERT` → رابط سير العمل 01
   - على جدول `profiles`، الحدث `INSERT` → رابط سير العمل 02
   - على جدول `returns`، الحدث `INSERT` → رابط سير العمل 03
3. من **Project Settings → API**، احفظ `Project URL` و`service_role key` (سرّي، لا يُستخدم إلا من n8n على الخادم، أبداً في الواجهة الأمامية).

### ب) n8n
1. استورد الملفات الثلاثة (`n8n-01-order-notification.json`, `n8n-02-referral-processing.json`, `n8n-03-blacklist-update.json`) عبر **Import from File**.
2. أضف متغيرات البيئة التالية في إعدادات n8n (Settings → Variables أو ملف `.env` حسب طريقة الاستضافة):
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `WHATSAPP_PHONE_NUMBER_ID`
   - `WHATSAPP_CLOUD_API_TOKEN`
3. فعّل كل سير عمل (Active)، وانسخ رابط كل Webhook node لاستخدامه في خطوة Supabase Webhooks أعلاه.
4. **استضافة n8n مجانية للبداية**: يمكن تشغيله على خادم صغير مجاني/رخيص (VPS)، أو حساب n8n Cloud التجريبي المجاني — القرار متروك لك حسب ما ورد في تعليماتك الأصلية باستعمال أدوات مجانية للبداية.

### ج) WhatsApp Cloud API
1. أنشئ تطبيق Meta for Developers واربطه برقم WhatsApp Business (تحقق ووثائق Meta الرسمية مطلوبة، هذه خطوة خارج نطاق هذه المنصة).
2. احصل على `Phone Number ID` و`Permanent Access Token` من لوحة تحكم التطبيق.
3. لم يتم تحديد مزوّد جزائري وسيط في تعليماتك — إن رغبت باستعمال مزوّد محلي بديل عن الربط المباشر مع Meta، هذا قرار يحتاج موافقتك قبل التنفيذ.

### د) Typebot
1. اتبع `typebot-customer-service-flow.md` لبناء التدفق يدوياً عبر محرر Typebot (الإصدار المجاني كافٍ للبداية).
2. أضف Webhook Blocks بالروابط المذكورة في الملف (تحتاج سير عمل n8n إضافي لكل من: البحث عن منتج، تتبع الطلب، وتسليم المحادثة — لم تُبنَ هذه الثلاثة بعد وتحتاج تفصيلاً منفصلاً بموافقتك، لتُبنى بنفس نمط الملفات الثلاثة الحالية).
3. ضمّن الكود الناتج من Typebot داخل `04-storefront.html`.

### هـ) Vercel
1. ادفع جميع ملفات HTML (`01-landing.html` … `09-contract.html`) إلى مستودع Git.
2. اربط المستودع بمشروع Vercel جديد (Static Site، بدون إعدادات بناء خاصة).
3. أضف نطاق `.dz` أو `.com` حسب ما تختاره لاحقاً.

---

## 3. نقص متعمّد — بانتظار موافقتك
تماشياً مع تعليماتك الصارمة بعدم الاجتهاد دون إذن، لم أُنشئ بعد:
- سير عمل n8n لثلاثة Webhooks التي يستدعيها Typebot (البحث عن منتج / تتبع الطلب / تسليم المحادثة) — تحتاج تفصيلاً كالملفات الثلاثة الحالية.
- ربط فعلي بمزوّد WhatsApp Cloud API جزائري وسيط (إن كنت تفضّله على الربط المباشر مع Meta).
- دمج Chargily/Payzone الفعلي (مفاتيح API حقيقية) — المخطط والواجهات جاهزة لاستقبالها، لكن الربط البرمجي الفعلي لم يُبنَ بعد.

أخبرني أي هذه النقاط تريد إكماله أولاً.
